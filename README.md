# Домашнее задание LLM-START

## Студент
**Чесноков Антон Николаевич**

## Описание
Репозиторий содержит выполнение домашнего задания после интенсива **LLM-START**.

## Проект
Разработка ИИ-ассистента в виде Telegram-бота для первичных консультаций клиентов по услугам компании.

### Основные функции
- Ответы на вопросы о компании и услугах через LLM (OpenRouter)
- Ведение контекстного диалога с сохранением истории
- Выявление потребностей клиентов
- Рекомендация подходящих решений

## Технологии
- **Python 3.11+** - основной язык разработки
- **aiogram** - асинхронная работа с Telegram Bot API
- **OpenRouter** - доступ к различным LLM (через OpenAI-совместимый клиент)
- **uv** - управление зависимостями и виртуальным окружением
- **Docker** - контейнеризация для деплоя

## Структура проекта
```
cursor-homework/
├── src/                 # Исходный код приложения
│   ├── __init__.py
│   ├── bot.py           # Точка входа, инициализация и запуск бота
│   ├── handlers.py      # Обработчики команд и сообщений
│   └── llm.py           # Интеграция с OpenRouter через OpenAI client
├── tests/               # Автоматические тесты
│   ├── __init__.py
│   ├── test_handlers.py
│   └── test_llm.py
├── doc/                 # Документация
│   ├── product-idea.md  # Описание идеи продукта
│   ├── vision.md        # Техническое видение
│   ├── tasklist.md      # План разработки
│   └── workflow.md      # Процесс разработки
├── .env                 # Переменные окружения (НЕ в git)
├── .env.example         # Шаблон переменных окружения
├── .gitignore
├── pyproject.toml       # Конфигурация uv и зависимости
├── Makefile             # Команды автоматизации
├── Dockerfile           # Образ для деплоя (будет добавлен позже)
└── README.md
```

## Установка и настройка

### Требования
- Python 3.11 или выше
- [uv](https://github.com/astral-sh/uv) - установщик зависимостей
- Docker (опционально, для деплоя)

### Установка uv
```bash
# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows (PowerShell)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### Настройка проекта

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd cursor-homework
```

2. **Установите зависимости:**
```bash
make install
```

3. **Создайте файл .env на основе примера:**
```bash
cp .env.example .env
```

4. **Заполните необходимые переменные в .env:**
   - `TELEGRAM_BOT_TOKEN` - токен от [@BotFather](https://t.me/BotFather)
   - `OPENROUTER_API_KEY` - API ключ от [OpenRouter](https://openrouter.ai/keys)
   - `SYSTEM_PROMPT` - системный промпт для LLM (описание компании и инструкции)

## Использование

### Доступные команды

```bash
make help       # Показать все доступные команды
make install    # Установить зависимости
make test       # Запустить тесты
make run        # Запустить бота локально
make build      # Собрать Docker образ
make up         # Запустить бота в Docker
make down       # Остановить Docker контейнер
make logs       # Показать логи Docker
make restart    # Перезапустить Docker контейнер
make clean      # Очистить кеш и временные файлы
```

### Запуск локально (без Docker)

```bash
# Убедитесь, что .env настроен
make run
```

Бот запустится в текущей терминальной сессии. Для остановки используйте `Ctrl+C`.

---

### Запуск в Docker (рекомендуется для production)

**Требования:** Docker и Docker Compose установлены на вашей системе.

#### Первый запуск:

```bash
# 1. Убедитесь, что .env файл настроен с вашими токенами
# 2. Соберите Docker образ
make build

# 3. Запустите контейнер
make up

# 4. Проверьте логи
make logs
```

Бот запустится в фоновом режиме и будет автоматически перезапускаться при сбоях.

#### Управление контейнером:

```bash
# Просмотр логов в реальном времени
make logs

# Перезапуск контейнера (после изменений в .env)
make restart

# Остановка контейнера
make down

# Пересборка и перезапуск (после изменений в коде)
make build && make down && make up
```

#### Проверка статуса:

```bash
# Проверить запущенные контейнеры
docker ps

# Проверить логи контейнера напрямую
docker logs telegram-bot-llm -f
```

#### Особенности Docker версии:

- ✅ **Автоматический перезапуск** - бот восстанавливается после сбоев
- ✅ **Изолированная среда** - не конфликтует с системным Python
- ✅ **Ротация логов** - автоматическая очистка старых логов (max 3 файла по 10MB)
- ✅ **Легкий деплой** - один образ на любой сервер с Docker

## Тестирование

Запуск автоматических тестов:
```bash
make test
```

## Статус разработки

🚧 **В разработке**

Прогресс выполнения итераций смотрите в [doc/tasklist.md](doc/tasklist.md)

## Документация

- [Идея продукта](doc/product-idea.md) - детальное описание концепции
- [Техническое видение](doc/vision.md) - архитектура и технологии
- [План разработки](doc/tasklist.md) - итерации и задачи
- [Workflow](doc/workflow.md) - процесс разработки

## Принципы разработки

- **KISS** - максимально простые решения
- **DRY** - избегаем дублирования кода
- **Читаемость** - код для людей, не машин
- **Минимум абстракций** - только необходимое

## Лицензия

Учебный проект для домашнего задания LLM-START

## Автор

Чесноков Антон Николаевич
