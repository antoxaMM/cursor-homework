# План разработки Telegram бота

## Прогресс выполнения

| Итерация | Статус | Дата завершения | Примечания |
|----------|--------|-----------------|------------|
| 0. Инициализация проекта | ✅ Завершено | 2026-01-07 | Структура проекта создана, зависимости установлены |
| 1. Минимальный бот | 🚧 В работе | - | - |
| 2. Интеграция с LLM | ⏳ Не начато | - | - |
| 3. История диалога | ⏳ Не начато | - | - |
| 4. Обработка ошибок | ⏳ Не начато | - | - |
| 5. Тесты | ⏳ Не начато | - | - |
| 6. Docker и деплой | ⏳ Не начато | - | - |

**Легенда статусов:**
- ⏳ Не начато
- 🚧 В работе
- ✅ Завершено
- ⚠️ Проблемы

---

## Итерация 0: Инициализация проекта

**Цель:** Подготовить структуру проекта и окружение для разработки

### Задачи:
- [x] Создать структуру каталогов согласно @doc/vision.md (src/, tests/, doc/)
- [x] Инициализировать `pyproject.toml` с зависимостями (aiogram, openai, python-dotenv, pytest)
- [x] Создать `.env.example` с шаблоном всех переменных окружения
- [x] Создать `.gitignore` (исключить .env, __pycache__, .pytest_cache, venv/)
- [x] Создать `Makefile` с базовыми командами (install, test, run)
- [x] Обновить `README.md` с инструкциями по установке и запуску

**Тест:** `make install` успешно устанавливает зависимости

---

## Итерация 1: Минимальный бот (эхо-бот)

**Цель:** Запустить простейшего Telegram бота, который отвечает на сообщения

### Задачи:
- [ ] Создать `src/__init__.py`
- [ ] Реализовать `src/bot.py` - инициализация aiogram бота и запуск polling
- [ ] Реализовать `src/handlers.py` - обработчики:
  - [ ] Команда `/start` - приветственное сообщение
  - [ ] Текстовые сообщения - эхо (повторить сообщение пользователя)
  - [ ] Нетекстовые сообщения - уведомление "Контент не распознан"
- [ ] Настроить базовое логирование (модуль logging, вывод в stdout)
- [ ] Создать `.env` файл с `TELEGRAM_BOT_TOKEN`

**Тест:** Отправить боту сообщение - он отвечает эхом. Отправить фото - получить уведомление.

---

## Итерация 2: Интеграция с LLM

**Цель:** Заменить эхо-ответы на ответы от LLM через OpenRouter

### Задачи:
- [ ] Реализовать `src/llm.py` - функция запроса к OpenRouter:
  - [ ] Инициализация OpenAI client с custom base_url (OpenRouter)
  - [ ] Функция `get_llm_response(messages)` - получение ответа от модели
- [ ] Добавить переменные в `.env` и `.env.example`:
  - [ ] `OPENROUTER_API_KEY`
  - [ ] `OPENROUTER_BASE_URL`
  - [ ] `LLM_MODEL`
  - [ ] `LLM_TEMPERATURE`
  - [ ] `LLM_MAX_TOKENS`
  - [ ] `SYSTEM_PROMPT`
- [ ] Изменить обработчик текстовых сообщений в `handlers.py`:
  - [ ] Вместо эха - запрос к LLM с системным промптом
  - [ ] Отправка ответа LLM пользователю

**Тест:** Отправить боту вопрос - получить осмысленный ответ от LLM.

---

## Итерация 3: История диалога

**Цель:** Добавить поддержку контекста диалога - бот помнит предыдущие сообщения

### Задачи:
- [ ] Создать in-memory хранилище в `handlers.py`:
  - [ ] `conversation_history = {chat_id: [messages]}` - история сообщений
  - [ ] `conversation_meta = {chat_id: {...}}` - метаданные (username, started_at)
- [ ] Обновить обработчик текстовых сообщений:
  - [ ] Сохранять сообщение пользователя в историю
  - [ ] Передавать историю в `llm.get_llm_response()`
  - [ ] Сохранять ответ LLM в историю
- [ ] Обновить `llm.py`:
  - [ ] Формировать messages для LLM: system + история (с учетом лимита)
  - [ ] Использовать `CONVERSATION_HISTORY_LIMIT` из .env
- [ ] Добавить `CONVERSATION_HISTORY_LIMIT` в `.env` и `.env.example`

**Тест:** Отправить боту несколько связанных сообщений - бот помнит контекст и отвечает с учетом истории.

---

## Итерация 4: Обработка ошибок и retry

**Цель:** Добавить обработку ошибок при запросах к LLM с автоматическими повторными попытками

### Задачи:
- [ ] Реализовать retry-логику в `llm.py`:
  - [ ] Обернуть запрос к LLM в `try-except`
  - [ ] При ошибке - повторить запрос (количество попыток из `LLM_RETRY_ATTEMPTS`)
  - [ ] Пауза между попытками: 1 секунда
  - [ ] Логировать каждую попытку (WARNING)
- [ ] Обработка ошибок в `handlers.py`:
  - [ ] При неудачных попытках LLM - отправить пользователю сообщение об ошибке
  - [ ] Логировать ошибку с полным traceback (ERROR)
- [ ] Добавить `LLM_RETRY_ATTEMPTS` в `.env` и `.env.example`
- [ ] Улучшить логирование:
  - [ ] Настроить формат логов (timestamp, level, message)
  - [ ] Добавить `LOG_LEVEL` в `.env`
  - [ ] Логировать все ключевые события согласно @doc/vision.md

**Тест:** Временно указать неверный API ключ - бот должен попытаться 3 раза, затем отправить сообщение об ошибке. Восстановить ключ - бот работает.

---

## Итерация 5: Тесты

**Цель:** Покрыть критичный функционал автоматическими тестами

### Задачи:
- [ ] Создать `tests/__init__.py`
- [ ] Реализовать `tests/test_handlers.py`:
  - [ ] Тест обработки команды `/start`
  - [ ] Тест обработки текстовых сообщений
  - [ ] Тест обработки нетекстовых сообщений
  - [ ] Тест сохранения истории диалога
- [ ] Реализовать `tests/test_llm.py`:
  - [ ] Тест формирования запроса к LLM (с историей и лимитом)
  - [ ] Тест обработки ответа от LLM
  - [ ] Мокировать API запросы (pytest-mock или unittest.mock)
- [ ] Обновить `Makefile` - команда `make test` запускает pytest
- [ ] Проверить работу всех тестов: `make test`

**Тест:** Запустить `make test` - все тесты проходят успешно.

---

## Итерация 6: Docker и деплой

**Цель:** Подготовить проект к деплою через Docker

### Задачи:
- [ ] Создать `Dockerfile`:
  - [ ] Базовый образ: `python:3.11-slim`
  - [ ] Установить uv
  - [ ] Копировать `pyproject.toml` и установить зависимости
  - [ ] Копировать исходный код (src/)
  - [ ] ENTRYPOINT: запуск бота
- [ ] Создать `docker-compose.yml`:
  - [ ] Сервис бота с автоматическим restart
  - [ ] Монтирование `.env` файла
  - [ ] Сбор логов
- [ ] Обновить `Makefile`:
  - [ ] `make build` - сборка Docker образа
  - [ ] `make up` - запуск через docker-compose
  - [ ] `make down` - остановка контейнера
  - [ ] `make logs` - просмотр логов
  - [ ] `make restart` - перезапуск
- [ ] Обновить `README.md`:
  - [ ] Инструкции по запуску через Docker
  - [ ] Описание всех make команд

**Тест:** Запустить `make build && make up` - бот запускается в контейнере и отвечает на сообщения. Проверить логи: `make logs`.

---

## Следующие шаги (после MVP)

- Добавить команду `/help` с описанием функций бота
- Реализовать очистку старых диалогов из памяти (TTL)
- Добавить метрики (Prometheus) для мониторинга
- Настроить CI/CD pipeline (GitHub Actions)
- Развернуть в облаке (AWS/GCP/DigitalOcean)

---

**Ссылки:**
- Техническое видение: @doc/vision.md
- Идея продукта: @doc/product-idea.md
- Соглашения: @conventions.md

